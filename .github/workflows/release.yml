name: Release

on:
  release:
    types: [published]

permissions:
  contents: write
  actions: write

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    with:
      run-checks: false
      upload-release: true
      release-tag: ${{ github.event.release.tag_name }}
